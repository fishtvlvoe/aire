## MODIFIED Requirements

### Requirement: land-form-multi-lot-rendering

The land disclosure form's "土地標示" section SHALL render all entries from `land_lots` as a list with `data-testid="land-lots-list"`.
