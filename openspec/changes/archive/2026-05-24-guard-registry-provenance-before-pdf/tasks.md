- [ ] 1.1 建立 `src/lib/registry-provenance.ts`，實作 provenance envelope 型別、trusted entry 轉換、legacy payload guard，覆蓋 Requirement: Registry payloads SHALL carry source provenance before PDF use 與 Requirement: Candidate and probe data SHALL NOT be trusted for PDF。驗證：`pnpm vitest run src/lib/__tests__/registry-provenance.test.ts`
- [ ] 1.2 修改 `src/components/PullParcelDataButton.tsx`，儲存與匯出謄本資料時使用 provenance envelope，成功 API 與人工確認資料才可 `trustedForPdf = true`，覆蓋 Requirement: Registry payloads SHALL carry source provenance before PDF use。驗證：`pnpm vitest run src/components/__tests__/PullParcelDataButton.test.tsx`
- [ ] 1.3 修改 `src/lib/pdf-engine/assemble-dossier-data.ts` 的 assembleDossierData，補件完成後只讀取 trusted provenance entries；舊版 raw payload、mock、candidate、raw_probe 不得填入已確認欄位；物調表階段需允許候選/公開資料但標示 pre_survey 與 pending confirmation，覆蓋 Requirement: Disclosure PDF SHALL only use trusted registry data。驗證：`pnpm vitest run src/lib/pdf-engine/__tests__/assemble-dossier-data.test.ts`
- [ ] 1.4 修改 `src/lib/registry-preview.ts`，預覽頁可讀 provenance envelope 的 trusted entries，避免 UI 看不到已確認資料，覆蓋 Requirement: Registry payloads SHALL carry source provenance before PDF use。驗證：`pnpm vitest run src/lib/__tests__/registry-preview.test.ts`
- [ ] 1.5 修改 `src-tauri/tests/cop_api_yunong_live.rs` 裕農路 live runner，summary 明確輸出 trusted success、candidate、unauthorized、raw probe 統計，並保留已知屋主姓名比對為 comparison-only，覆蓋 Requirement: Live MOI runner SHALL separate candidate inputs from official API results 與 Requirement: Live API evidence SHALL classify blocked states。驗證：`cd src-tauri && cargo test --test cop_api_yunong_live`
- [ ] 1.6 執行 SR gate：`spectra analyze guard-registry-provenance-before-pdf --json` 與 `spectra validate guard-registry-provenance-before-pdf` 需 0 Critical/Warning。
