# Phase 0 Spike Report — upload-first-autofill

> 驗證 design.md「Phase 0 Spike 假設與結果」表中的假設 A 與 B。
> 樣本：陳世曉-謄本.pdf（Fish 提供，僅本機 spike 使用，不入版控）
> 日期：2026-04-24

## 假設 A：pdfjs-dist 能從真實謄本抽出中文文字層

**狀態**：✅ PASS
**方法**：`pdfjs.getDocument({ data, useSystemFonts: true }).getTextContent()`

### 結果

- 頁數：2（page 1 = 土地謄本 / page 2 = 建物謄本，單一 PDF 合併兩份）
- 有無文字層：**有**（非掃描影像）
- 總字數：1975（page 1: 982 / page 2: 993）
- 中文可讀性：**100% 可讀，無亂碼**
- 原始輸出節錄（page 1 前 80 字）：
  > 土地登記第一類謄本（所有權個人全部）永康區大灣段 6338-0000地號列印時間：民國115年04月02日17時13分

### 結論

Layer 1（pdfjs 文字層）對電子謄本完全可行。Phase 1 不需改走 OCR。

**額外發現**：
- 謄本大量使用 `*` 填充字元對齊（例：`權利範圍：*****10000分之91********`），parser 必須先 strip `*`
- 一份 PDF 可能同時包含土地 + 建物謄本兩頁 → **pipeline 需要「頁面分類」步驟**，不能假設一個檔案一種類型
- 金額/面積以全形半形混用（例：`35,000.0元／平方公尺`，全形斜線），regex 要注意

---

## 假設 B：5 行 regex 抽 4 核心欄位，命中 ≥ 3/4

**狀態**：✅ PASS（8 個欄位命中 7 個，87.5%）
**方法**：對 `getTextContent()` 輸出直接跑 regex，無預處理。

### Regex 版本 v1
```js
const regex = {
  section: /([\u4e00-\u9fa5]+段)\s*(\d{4,5}-\d{3,4})(?:地號|建號)/,
  area:    /(?:面\s*積|總面積)：\*+([\d,]+\.\d+)\s*平方公尺/,
  rights:  /權利範圍：\**([^\n*]+?)\*+/,
};
```

### 命中結果

| 頁 | 欄位 | 抽出值 | 正確值（人工） | 命中 |
|----|------|--------|--------------|------|
| 1（土地） | 地段 | 永康區大灣段 | 永康區大灣段 | ✅ |
| 1（土地） | 地號 | 6338-0000 | 6338-0000 | ✅ |
| 1（土地） | 面積 | 1,223.00 平方公尺 | 1,223.00 平方公尺 | ✅ |
| 1（土地） | 權利範圍 | 10000分之91 | 10000分之91 | ✅ |
| 2（建物） | 地段 | 永康區大灣段 | 永康區大灣段 | ✅ |
| 2（建物） | 建號 | 07615-000 | 07615-000 | ✅ |
| 2（建物） | 面積 | 84.13 平方公尺 | 84.13 平方公尺 | ✅ |
| 2（建物） | 權利範圍 | （空） | 全部（1分之1） | ❌ |

**命中率：7/8 = 87.5%**

### 結論

Regex 路線可行。未命中的 1 個是格式變異：
- Pattern A：`權利範圍：*****10000分之91********`（純分數）→ v1 regex 能抓
- Pattern B：`權利範圍：全部 *********1分之1*********`（文字 + 分數混合）→ v1 regex miss

Phase 1.4/1.5 parser 需擴充處理 Pattern B，並把「全部」「1分之1」分別 normalize 成 `rights.text = "全部"` + `rights.fraction = "1/1"`。

---

## Gate 決策

- [x] **Phase 1 照原計畫跑**（核心假設通過）
- [ ] Phase 1 需要微調
- [ ] Phase 1 需要大改

### 微幅補強（寫入 tasks.md Phase 1）

1. **新增前置步驟**：Task 1.3 抽文字後先做 `stripFillerChars(text)` — 去除 `*` 填充與全形空白。
2. **新增頁面分類**：Task 1.3 與 1.4/1.5 之間加「headerDetect」— 依「土地登記第一類謄本」/「建物登記第一類謄本」分類，再送對應 parser。
3. **Task 1.4 權利範圍** regex 必須同時處理兩種 pattern（純分數 vs 文字+分數）。

這 3 點不改 Phase 結構、不延長時程，只是 implementation 細節補強。Phase 2-6 完全不動。

### 仍待驗證（非今日 spike 範圍）

- 假設 C（PaddleOCR Docker）— 延至 Phase 2 啟動前
- 假設 D（業務信任度）— 延至 Phase 5 客戶驗收
- **新增假設 E**：謄本若為掃描 PDF（非電子）時 pdfjs 行為？今日樣本是電子謄本，客戶實際流程可能有紙本掃描，需再索取 1 份掃描樣本驗證。

---

## 附加分析：26 份 YAML 黃金樣本（`sample-inventory.md`）

Fish 後來補給 `/Users/fishtv/Downloads/本/` 資料夾內 26 份已解析的 YAML，派 Kimi 全讀後產出完整欄位清單與格式變異報告（存 `sample-inventory.md`）。關鍵結論：

### 1. 真實結構是 section-based，不是「一檔一種類型」

- 26 份樣本：14 份純土地、3 份純建物、9 份含他項權利
- 單一 PDF 可能同時含 6 個 section：土地標示部 / 土地所有權部 / 土地他項權利部 / 建物標示部 / 建物所有權部 / 建物他項權利部
- **Phase 1 需新增 section-splitter + MixedParser 組合器**

### 2. 原設計有 15 個未列欄位（出現率 ≥ 50%）

都在土地 section：使用分區、使用地類別、公告現值年月、公告地價年月、前次移轉現值、原因發生日期、地上建物建號[list]、地址、查詢時間、權狀字號、當期申報地價、當期申報地價年月、登記原因、登記日期、登記次序、統一編號。

### 3. 原設計有 7 個欄位名稱錯誤

| 原設計 | 真實名稱 |
|-------|---------|
| 公告現值 | **公告土地現值** |
| 坐落地址 | **建物門牌** |
| 建物面積 | **總面積** |
| 結構 | **主要建材** |
| 建築完成日 | **建築完成日期** |
| 地段（獨立） | 地段**內嵌於地號字串**（例：`臺南市下營區十六甲段2195-0000地號`）|
| 他項權利（欄位）| **土地他項權利部**（獨立 section）|

### 4. 需要跨 parser 共用的 normalize 層

日期（民國→西元）、面積（除,去單位→float）、地價（除,去單位→int）、權利範圍（X分之Y→Y/X）、空值（(空白)→null）、層數（010層→10）、地號結構化拆解。

### 影響

- **tasks.md 已更新**：Phase 1 新增 Task 1.3b（text cleanup）、1.3c（section splitter）、1.4a（normalize 共用層）、1.5b（mixed parser 組合器）
- **design.md 已更新**：D8 改 3 種 parser + 完整欄位清單；新增 D9「樣本驅動的測試策略」（以 26 份 YAML 當 golden fixture）
- **Phase 1 預估時程**：從 2 週 → **約 2.5-3 週**（欄位量從 15 個擴到 60+，加 normalize 層）。
